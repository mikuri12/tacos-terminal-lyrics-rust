# tacos-lyrics theme — generated by noctalia
#
# The sung words use the scheme's PRIMARY color; the rest uses "dim"
# (the terminal's own text color, faint) so it can never blend into the
# background, whatever the palette is.
#
# tacos-lyrics reloads this file on SIGUSR1, so when noctalia regenerates
# it after a theme change the running visualizer recolors instantly.

sung   = {{colors.primary.default.hex}}
unsung = dim
paused = dim
header = dim

# Prefer the whole line in the primary color (no karaoke contrast)?
# Change unsung to the primary too:
# unsung = {{colors.primary.default.hex}}
