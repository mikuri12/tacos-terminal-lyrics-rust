# tacos-lyrics theme
# Generated by noctalia from the current colorscheme.
#
# Values: #rrggbb | palette:N | default | dim
#   sung   -> words already sung (karaoke)
#   unsung -> words not yet sung
#   paused -> the whole line while playback is paused
#   header -> the "artist - title" line
#
# tacos-lyrics reloads this file on SIGUSR1, so when noctalia regenerates
# it after a theme change the running visualizer recolors instantly.

sung   = {{colors.primary.default.hex}}
unsung = {{colors.on_surface_variant.default.hex}}
paused = {{colors.on_surface_variant.default.hex}}
header = {{colors.outline.default.hex}}
